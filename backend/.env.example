# TechnoSpirit backend — Phase 2 configuration template.
# Nothing here is read yet; the Phase 1 backend is an empty scaffold.

NODE_ENV=development
PORT=5000

# Database
MONGODB_URI=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Mail
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

# Client
CLIENT_ORIGIN=http://localhost:5173
